{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "header-max-length": [2, "always", 1000000],
    "scope-enum": [2, "always", ["pipeline", "core", "flow", "styleguide", "fas"]],
    "subject-case": [2, "always", "sentence-case"]
  }
}
